# Object edits

- Inline edits is for object explorer, not the object table in workshop. We configure the ability to make inline edits in Properties -> Click on property -> Interaction -> Inline edit
- To edit a row in object table, customize right-click menu and configure one of the options to edit
- Configuring default values in form involves setting the object type as the default value, not the individual properties